let nu1m;
let num2;
let resulado;

function sobra() {
    nu1m = Number(window.prompt("digite o primeiro numero:"));
    num2 = Number(window.prompt("digite o segundo numero:"));

    resulado = num1 % num2;

    alert("o resultado é: " + resulado + ".");
}


function multiplicacao() {
    nu1m = Number(window.prompt("digite o primeiro numero:"));
    num2 = Number(window.prompt("digite o segundo numero:"));

    resulado = num1 * num2;

    alert("o resultado é: " + resulado + ".");
}


function divisao() {
    nu1m = Number(window.prompt("digite o primeiro numero:"));
    num2 = Number(window.prompt("digite o segundo numero:"));

    resulado = num1 / num2;

    alert("o resultado é: " + resulado + ".");
}


function subtracao() {
    nu1m = Number(window.prompt("digite o primeiro numero:"));
    num2 = Number(window.prompt("digite o segundo numero:"));

    resulado = num1 - num2;

    alert("o resultado é: " + resulado + ".");
}

function adicao() {
    nu1m = Number(window.prompt("digite o primeiro numero:"));
    num2 = Number(window.prompt("digite o segundo numero:"));

    resulado = num1 + num2;

    alert("o resultado é: " + resulado + ".");
}